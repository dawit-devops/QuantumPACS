# pacs_admin — Gap Analysis (Phase 2)
Date: 2026-08-28
Sources: RBAC_matrix_spec.md §5 (Matrix A, PACSADM column, parsed programmatically), permissions.py MATRIX_A_PACSADM, navigator.ts, Sidebar.tsx, backend route gates
Skills invoked: iam-audit (least privilege, centralized can(), dead grants), multi-tenant-saas (tenant scoping)

## Gaps

| # | Surface | Documented (ADR/spec) | Actual (code) | Severity | Evidence (file:line) | Notes |
|---|---|---|---|---|---|---|
| G1 | PACS-ops surfaces (DICOMweb console, HL7, Interface Health, Replicas, Routing) | Spec Matrix A PACSADM grants STORAGE_ADMIN, INTERFACE_ADMIN, INTERFACE_MONITOR — the ops grants that should unlock DICOM/storage/interface management | Those three grants gate **zero** backend endpoints. The actual surfaces gate on DICOMWEB_READ / HL7_READ / REPLICA_READ / ROUTING_READ — none of which pacs_admin holds → the role named "PACS admin" cannot reach the DICOMweb console, HL7 console, Interface Health, Replicas, or Routing at all | HIGH | permissions.py:252-262 (grant set), backend/api/dicomweb.py:147, hl7_admin.py:23, replicas.py:34 (gates); grep: STORAGE_ADMIN/INTERFACE_ADMIN/INTERFACE_MONITOR have no gate sites | Identity mismatch: facility PACS administrator per PRD persona, but cannot operate PACS infrastructure |
| G2 | Billing (7 surfaces) | Spec Matrix A PACSADM = **NO** BILLING_READ (blank at col 10) | Code MATRIX_A_PACSADM grants BILLING_READ → pacs_admin sees all 7 billing surfaces (Queue/Claims/Revenue/Unbilled/Denials/Fee Schedule/Reconciliation) | MEDIUM | docs/reaserch/RBAC_matrix_spec.md:161 (BILLING_READ blank for PACSADM), permissions.py:254 ('BILLING_READ' in MATRIX_A_PACSADM) | Spec drift + least-privilege: a PACS admin reading revenue/denials/aging is not spec'd. Compare: INFO column has BILLING_READ, PACSADM doesn't |
| G3 | CRITICAL_RESULTS_WRITE | Spec Matrix A PACSADM = **NO** (blank) | Code grants CRITICAL_RESULTS_WRITE; used only by /exams/{id}/critical-flag (exam console, acquisition workspace) which is clinical-scoped and hidden from admin-scoped pacs_admin → unreachable dead grant | LOW | RBAC_matrix_spec.md:162, permissions.py:258, backend/api/exams.py:712, navigator.ts:49-55 (ADMIN_SCOPED_ROLES hides acquisition) | Spec drift + dead grant. pacs_admin can never flag a critical result from any reachable surface |
| G4 | REPORT_TEMPLATE_ADMIN | Spec Matrix A PACSADM = **NO** (blank) | Code grants REPORT_TEMPLATE_ADMIN; this IS exercised — /admin/report-templates gates on REPORT_WRITE\|REPORT_TEMPLATE_ADMIN and is in the Admin section (reachable) | LOW | RBAC_matrix_spec.md:163, permissions.py:258, frontend/src/common/Sidebar.tsx:456-464 | Spec drift, but grant is live and intentional (matches tenant_admin O1/R3 work) — code is more permissive than spec |
| G5 | ROLE_READ/WRITE/DELETE | Spec Matrix A PACSADM column: no ROLE_* listed (only USER_READ/USER_WRITE) | Code grants ROLE_READ/WRITE/DELETE with R2-16 rationale "facility admins manage roles of clinical/operational built-ins" — exercised via /roles | LOW | permissions.py:259-261, navigator: no conflict | Deliberate extension beyond spec (R2-16); spec should be updated to reflect it |
| G6 | Dead clinical grants (SCHEDULE_READ, REPORT_READ, WORKLIST_READ/WRITE, VIEWER_READ, STUDY_READ, FILE_READ/WRITE, PATIENT_READ, ORDER_READ, CHART/RESULTS_READ) | Spec Matrix A PACSADM grants these | Most are unreachable via nav because clinical workspaces are hidden for admin-scoped roles. Exceptions that ARE exercised from admin surfaces: REPORT_READ (RIS Dashboard gate), SCHEDULE_READ (Staff Schedule gate). WORKLIST_WRITE has no reachable surface | LOW | navigator.ts:49-55, 84-89 (NON_ADMIN_WORKSPACES), Sidebar.tsx:924-926 | Not necessarily wrong (grants are the union that makes admin surfaces work), but WORKLIST_WRITE + critical/clinical write grants are dead |
| G7 | `_is_platform_admin` legacy `user.admin` boolean | iam-audit M-2: legacy `users.admin` is a parallel super-admin path outside RBAC | Same pattern as tenant_admin G4: api/tenants.py:35-38 uses `user.admin` not role check. pacs_admin (admin=false) is correctly blocked from tenant provisioning, but the pattern is fragile | MEDIUM | backend/api/tenants.py:35-38, docs/iam-audit.md | Cross-cutting, already tracked as BL-001 (tenant_admin backlog). Record here for completeness |
| G8 | Tenant scoping | test.pacs_admin is platform-side (tenant NULL) | No tenant scope on the test credential; tenant-scoped surfaces (if any) show cross-tenant platform data — not a leak, just dev-credential skew | PASS | backend/seed_test_users.py:53 | Not a real gap; note for Phase 5 walk |

## No gap (verified)
- Storage/interface grants intentionally dropped from DICOMweb sidebar gate (comment Sidebar.tsx:633-639) — but this is exactly WHY G1 is a gap: the grants were dropped because the route needs DICOMWEB_READ, which the role was never given.

## Summary of decision candidates (Phase 3)
1. **G1 (HIGH)** — decide the PACS-admin identity: either (a) grant DICOMWEB_READ/HL7_READ/REPLICA_READ/ROUTING_READ to pacs_admin so the role can operate PACS infrastructure (REDESIGN of grant set), or (b) KEEP the role as a users/roles/logs/staff/billing admin and rename scope (docs drift — PRD says "PACS administrator").
2. **G2 (MEDIUM)** — BILLING_READ: spec says NO. Either trim from code (align to spec) or update spec (billing read for facility admin).
3. **G3/G4 (LOW)** — spec drift on CRITICAL_RESULTS_WRITE (dead, trim or update spec) and REPORT_TEMPLATE_ADMIN (live, update spec).
4. **G5 (LOW)** — update spec to include ROLE_* for PACSADM (R2-16).
5. **G6 (LOW)** — audit dead clinical grants; likely KEEP (union needed for admin surfaces) + trim WORKLIST_WRITE if never exercised.
6. **G7 (MEDIUM)** — already backlogged (BL-001); no action here.
# QuantumPACS Docker Compose environment variables
# Copy this file to .env and adjust values:
#   cp .env.example .env

POSTGRES_USER=quantumpacs
POSTGRES_PASSWORD=pa55w0rd
POSTGRES_DB=quantumpacs
POSTGRES_PORT=5432
REDIS_PORT=6379
BACKEND_PORT=8080
FRONTEND_PORT=80
# SECRET=replace-with-strong-random-secret-in-production
# BACKEND_HOST defaults to "localhost:8080" — override only for custom proxy config